{
  "name": "react-broker",
  "version": "1.1.4",
  "description": "Component lazy-loading + code splitting that works with SSR, Webpack 4, and Babel 7",
  "main": "dist/cjs/index.js",
  "browser": "dist/es/index.js",
  "module": "dist/node-es/index.js",
  "jsnext:main": "dist/es/index.js",
  "esnext": "dist/es/index.js",
  "repository": "https://github.com/jaredLunde/react-broker",
  "author": "Jared Lunde",
  "license": "MIT",
  "sideEffects": false,
  "private": false,
  "devDependencies": {
    "@stellar-apps/babel-preset-es": "^1.0.1",
    "@stellar-apps/babel-preset-react": "^1.0.0",
    "prettier": "^1.14.2",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "@babel/runtime": "^7.3.1",
    "@babel/runtime-corejs2": "^7.3.1",
    "babel-plugin-macros": "^2.4.0",
    "cdll-memoize": "^0.1.5",
    "empty": "^0.10.1"
  },
  "peerDependencies": {
    "prop-types": "^15.7.1",
    "react": ">= 16.3.0",
    "react-dom": ">= 16.3.0"
  },
  "scripts": {
    "build": "npm run build:es && npm run build:cjs && npm run build:node-es",
    "build:es": "npm run build-lazy:es",
    "build:node-es": "npm run build-lazy:node-es",
    "build:cjs": "npm run build-lazy:cjs && npm run build-macro",
    "build-lazy:es": "rimraf dist/es && cross-env NODE_ENV=production BABEL_ENV=es babel src/lazy --out-dir dist/es && npm run prettier:es",
    "build-lazy:node-es": "rimraf dist/node-es && cross-env NODE_ENV=production BABEL_ENV=node-es babel src/lazy --out-dir dist/node-es",
    "build-lazy:cjs": "rimraf dist/cjs && cross-env NODE_ENV=production BABEL_ENV=cjs babel src/lazy --out-dir dist/cjs && npm run prettier:cjs",
    "build-macro": "rimraf macro && cross-env NODE_ENV=production BABEL_ENV=cjs babel src/macro --out-dir macro && npm run prettier-macro",
    "watch": "rimraf dist/es && cross-env NODE_ENV=development BABEL_ENV=es babel ./src/lazy -w --out-dir dist/es",
    "watch-macro": "rimraf macro && cross-env NODE_ENV=development BABEL_ENV=cjs babel ./src/macro -w --out-dir macro",
    "prettier:es": "prettier --single-quote --no-semi --no-bracket-spacing --trailing-comma es5 --write --tab-width 2 \"dist/es/**/*.js\"",
    "prettier:cjs": "prettier --single-quote --no-semi --no-bracket-spacing --trailing-comma es5 --write --tab-width 2 \"dist/cjs/**/*.js\"",
    "prettier-macro": "prettier --single-quote --no-semi --no-bracket-spacing --trailing-comma es5 --write --tab-width 2 \"macro/**/*.js\""
  },
  "keywords": [
    "react",
    "async component",
    "universal app",
    "pwa",
    "universal component",
    "ssr",
    "server side render",
    "code splitting",
    "babel-plugin-macros",
    "macro"
  ]
}
